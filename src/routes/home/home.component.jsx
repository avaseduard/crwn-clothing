import Directory from '../../components/directory/directory.component';

// Home page categories
const Home = () => {
    return (
    <div>
      <Directory />
    </div>
  );
};

export default Home;
